<!-- Already wired into the profile README at the repo root. Paths are relative to the repo root. -->

<p align="center">
  <picture>
    <source media="(prefers-color-scheme: dark)" srcset="profile-banner/assets/dark.svg">
    <source media="(prefers-color-scheme: light)" srcset="profile-banner/assets/light.svg">
    <img alt="Eshwar — AI/ML Engineer" src="profile-banner/assets/dark.svg" width="100%">
  </picture>
</p>

<!--
Notes
- GitHub serves README images through its image proxy, so SMIL animations play but
  :hover effects and links inside the SVG do not. Add real links as markdown below the banner.
- If the banner ever fails to animate in a cached view, hard-refresh or bump the filename
  (dark.svg -> dark-v2.svg) to bust the proxy cache.
-->
